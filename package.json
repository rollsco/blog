{
  "name": "blog",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "compress-images": "^1.5.8"
  },
  "scripts": {
    "compress-images": "rm -r /tmp/img.backup; mv public/img /tmp/img.backup; node services/compressImages.js"
  }
}
